# The .env file where to store information such as the Bintray credentials
# See .env.sample for a template
.env

# RSpec generates this to persist some state between runs in order to support
# the `--only-failures` and `--next-failure` CLI options. We recommend
spec/examples.txt

# These are the files generated by the YARD documentation tool
.yardoc/
doc/
