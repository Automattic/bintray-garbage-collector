version: 2.1

jobs:
  hello:
    docker:
      - image: alpine
    steps:
      - run: echo "Hello, world! 👋"

workflows:
  welcome:
    triggers:
      - schedule:
          # Every day, at 0 minute and 0 hour (midnight)
          cron: "0 0 * * *"
          filters:
            branches:
              only:
                - trunk
    jobs:
      - hello
